# Word lists of 100 words for 14 languages

The CSV files are formatted for direct import into Google Sheets or Excel (UTF-8 with BOM, semicolon delimiter, European decimal format). Each file includes the word, its tier weight.
Excel files provided for convenience as well.
